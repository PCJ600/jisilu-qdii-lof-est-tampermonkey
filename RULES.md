# 业务规则与维护说明

本文档是 **集思录 QDII 估值油猴脚本** 的规格说明，供后期改逻辑、对口径、写单测时对照。用户向安装说明见 [README.md](README.md)。

**代码对应关系**

| 文档概念 | 亚洲 `#qdiia` | 欧美/商品 `#qdiie` |
|----------|---------------|---------------------|
| 纯计算 | `jisilu-qdiia-lof-calc.js` | `jisilu-qdiie-t1-calc.js` |
| 页面脚本 | `jisilu-qdiia-lof-estimate.user.js` | `jisilu-qdiie-t1-estimate.user.js` |
| 单测 | `jisilu-qdiia-lof-calc.test.js` | `jisilu-qdiie-t1-calc.test.js` |

当前脚本版本（以 `@version` / 面板为准）：亚洲 **v1.4.1**，T-1 **v1.1.1**。

---

## 1. 共同前提

### 1.1 数据来源与范围

- **只读** 集思录当前页 DOM（现价、净值、指数涨幅、净值日期等），**不** 调用 `qdii_list` 等接口。
- **股票仓位** 固定 **95%**；**不考虑汇率**（与集思录官方 IOPV、含汇率 T-1 估值可能系统性偏差）。
- **LOF / ETF** 在同一表格内用 **同一套公式**；未单独走 IOPV 列（ETF 改进见 §5）。
- 判断均为 **按行**：同表内可有的基金净值已更新、有的未更新。

### 1.2 时区与会话日（Session Day）

所有「今天」「会话日」均以 **`Asia/Shanghai`** 日历为准。

```
若 上海日历 为 周六 → 会话日 = 周五（自然日 −1）
若 上海日历 为 周日 → 会话日 = 周五（自然日 −2）
若 上海日历 为 周一至周五 → 会话日 = 当天自然日
```

**当前未实现**：A 股 **法定节假日**（春节、国庆等）。休市日仍可能被当作「周一至周五的会话日」，与真实交易周不一致（§5.1）。

实现函数：`getSessionDayYmd()`（两个 calc 模块中各有一份，逻辑应保持一致）。

### 1.3 净值日期解析

表头列名均为 **「净值日期」**，含义随市场不同（见 §3、§4）。解析为 **`YYYY-MM-DD`** 后再与会话日比较。

支持格式（实现：`parseNavDateYmd(raw, refSessionYmd)`）：

| 格式 | 示例 | 说明 |
|------|------|------|
| `YYYY-MM-DD` | `2026-07-23` | 直接使用 |
| `YY-MM-DD` | `26-07-23` | 两位年：`≥70 → 19xx`，否则 `20xx` |
| `MM-DD` | `07-23` | 用 `refSessionYmd` 补年；跨年用简单启发（`inferYearForMonthDay`） |

解析失败或为 `-` / `--`：

- **亚洲**：无法做「净值日期停估」判断 → **仍尝试指数估算**（与「有日期且触发停估」区分）。
- **T-1**：同上 → **仍尝试** `T-2 × 指数`（风险：日期缺失时周末误估需靠后续改进）。

### 1.4 指数涨幅不可用

单元格为 `-`、`--` 或无法解析为百分数 → **`isIndexUnavailable === true`**。

- **两列均显示 `-`**（不是 `--`）。
- **不参与** 净值日期逻辑（优先级最高）。
- 悬停 **title** 说明原因（跨市场、商品非会员无「参考标的期间涨幅」等）。

### 1.5 停估时的展示（v1.4.1 / v1.1.1 起）

一旦判定 **不应再按指数模型估估值/溢价**：

- **估值列、溢价列都显示 `-`**（不再用「公布净值」或「T-2 净值」代算溢价）。
- 清除溢价列颜色、`data-premium-val`（排序用）。
- **悬停 title** 必须包含 **「不计算原因」** 及关键日期，便于用户理解。

可估算时，溢价列仍用红/绿着色；title 保留公式与中间值。

---

## 2. 溢价率分母（可估算时）

| 市场脚本 | 估值列 | 溢价公式 |
|----------|--------|----------|
| 亚洲 | 实时估值 \(E\) | \((现价 - E) / E \times 100\%\) |
| 欧美/商品 | T-1日估值 \(E_{T1}\) | \((现价 - E_{T1}) / E_{T1} \times 100\%\) |

\(E\) / \(E_{T1}\) 计算见下文；**现价** 均为表格「现价」（场内最近成交价，休市后不更新）。

---

## 3. 亚洲市场（港股等）· `#qdiia` / `#flex_qdiia`

### 3.1 业务背景（与欧美差异）

- 时差相对 A 股小，集思录表用 **「净值」+「指数涨幅」** 表达 **当日/session 内** 的跟踪，语义接近 **「用已公布净值 + 当日指数补估实时净值」**。
- 表头 **净值** = 最近公布的单位净值；**指数涨幅** = 跟踪标的当日（或页面定义的）涨跌。
- **无 T-2 / T-1 列名**；停估逻辑绑在 **净值日期 vs 会话日**，不是 T-2 滚动。

### 3.2 计算公式（可估算时）

```text
实时估值 = 净值 × (1 + 指数涨幅 × 95%)
实时溢价率 = (现价 − 实时估值) / 实时估值 × 100%
```

- `指数涨幅`：小数形式，如 `-0.47%` → `-0.0047`。
- 实现：`calcEstimate(nav, indexRatio)`，`calcPremium(price, estimate)`。

### 3.3 何时两列均为 `-`

| 条件 | 函数/判断 | 说明 |
|------|-----------|------|
| 指数不可用 | `isIndexUnavailable` | 如部分日经 ETF |
| 净值日期已覆盖会话日 | `navDateYmd >= sessionDayYmd`（`isNavCoversSessionDay`） | 晚间净值更新后，净值已含该会话日行情；再乘指数 **重复计价**；相对该估算的溢价也无意义 |

**典型时间线**

- **交易日盘中**：净值日期多为 **上一交易日** → `navDate < sessionDay` → **估算**。
- **当日净值公布后**：净值日期变为 **当日或更晚** → `navDate >= sessionDay` → **两列 `-`**。
- **周六/日查看**：会话日回退 **周五**；若净值日期已是 **周五**，则停估；若仍为 **周四** 且指数列仍有意义，是否估算取决于指数列在休市日的展示（集思录可能为 `-` 或陈旧值）。

### 3.4 与欧美停估规则对比（重要）

| 项目 | 亚洲（港股等） | 欧美/商品 T-1 |
|------|----------------|---------------|
| 净值列 | **净值** | **T-2净值** |
| 净值日期含义 | 该 **净值** 的结账日 | 该 **T-2净值** 的结账日 |
| 停估条件 | `navDate >= sessionDay` | `navDate >= sessionDay - 1 自然日` |
| 停估阈值（周五会话） | 净值日期 **≥ 周五** 停 | 净值日期 **≥ 周四** 停 |
| 设计意图 | 公布净值已含「会话日」 | T-2 列滚到「会话日−1」时 T-2×T-1指数 模型失效 |

---

## 4. 欧美 / 商品 · `#qdiie` / `#flex_qdiie` + `#flex_qdiic`

### 4.1 业务背景

- QDII **时差** 大，集思录用 **T-2 净值 + T-1 指数涨幅** 估 **T-1 日估值**（非 A 股「当日实时」同一套语义）。
- **同一 hash `#qdiie`** 下有两个 FlexGrid，脚本 **分别增强**：
  - `flex_qdiie`：欧美指数 QDII
  - `flex_qdiic`：商品 QDII

### 4.2 列映射

| 表格 | 净值列 | 指数列 | 备注 |
|------|--------|--------|------|
| 欧美 | T-2净值（排除含「溢价」表头） | T-1指数涨幅 | |
| 商品 | T-2净值 | **参考标的期间涨幅** | 常需 **会员**；非会员多为 `-` |

### 4.3 计算公式（可估算时）

```text
T-1日估值 = T-2净值 × (1 + 指数涨幅 × 95%)
T-1日估值溢价率 = (现价 − T-1日估值) / T-1日估值 × 100%
```

- 实现：`calcT1Estimate(t2Nav, indexRatio)`。
- 商品与欧美 **同一公式**，仅指数列不同。

### 4.4 何时两列均为 `-`

| 条件 | 判断 | 说明 |
|------|------|------|
| 指数不可用 | `isIndexUnavailable` | 商品非会员等 |
| T-2 净值日期过新 | `!shouldUseT1IndexEstimate` | 见下 |

**T-1 停估规则（方案 B，已实现）**

```text
cutoff = 会话日 − 1 个自然日（日历减一天）
若 T-2净值日期 >= cutoff → 停估（两列 -）
若 T-2净值日期 <  cutoff → 可估算
```

等价：**仅当净值日期严格早于 cutoff 时才估**。

**示例（会话日 = 周五 2026-03-20）**

| T-2净值日期 | cutoff (=周四 03-19) | 结果 |
|-------------|----------------------|------|
| 周三 03-18 | 03-18 < 03-19 | 估算 |
| 周四 03-19 | 03-19 < 03-19 为假 | **停估** |
| 周末再看，T-2 已滚到周四 | 同上 | **停估**（T-1 估值/溢价均无意义；真实折溢价需「更新后净值 × 后续交易日指数」等，本脚本 **不展示**） |

### 4.5 为何与亚洲阈值不同

- 亚洲 **净值** 对应当前披露的最新 official NAV，用 **`navDate >= sessionDay`** 表示「已含会话日」。
- 欧美表显式 **T-2**，当 **净值日期滚到 sessionDay−1** 时，再用 **T-1 指数** 乘 T-2 会与页面 T-2/T-1 语义错位，故用 **更紧一天** 的 cutoff。

---

## 5. 后期改进方向

维护时可按优先级迭代；改规则时 **先改 calc + 单测，再同步 user.js**，并更新本文档版本说明。

### 5.1 A 股交易日历（高优先级）

**问题**：周末已用「回退周五」，但 **春节、国庆等** 仍把休市日当普通周五/周一，会话日错误 → 停估/估算边界偏移。

**方向**：

- 在仓库维护 **`trading-calendar-YYYY.json`**（上交所休市日）或内嵌当年数组。
- `getSessionDayYmd(now)` 改为：若 `now` 非交易日，则会话日 = **上一交易日**（而非简单周末回退）。
- 亚洲与 T-1 **共用** 同一日历模块，避免两套会话日。

### 5.2 T-2 / T-1 与基金会计日对齐（欧美/商品）

**问题**：

- 页面 **T-2、T-1** 是集思录命名，与各基金 **实际公布时点**（早晚差异）不完全一致。
- 周末 **T-2 净值日期滚动** 后，用户期望的「有效溢价」可能是 **周四净值 × (1 + 周五指数×95%)** 等，当前策略是 **直接停估**，避免误导。

**方向**：

- 根据 **净值日期 + 会话日 + 是否交易日** 切换模式：`ESTIMATE_T1` / `STALE` / `POST_ROLL`。
- 可选：在 `POST_ROLL` 用 **新 T-2 + 会话日指数** 展示第三列「参考估值」（需产品确认，避免与 T-1 列混淆）。
- 按 **基金代码** 配置公布滞后（工作量大，仅在有数据源时考虑）。

### 5.3 亚洲：净值公布时点与指数列语义

**问题**：闭市后、跨日、指数列仍变动时，`navDate >= sessionDay` 停估但指数未 `-` 的边界 case。

**方向**：

- 结合 **A 股交易时段**（9:30–15:00）在非会员无自动刷新时提示手动刷新。
- 若页面存在 **估值日期** 等列，可读 DOM 辅助停估（需真页核对表头）。

### 5.4 模型精度（与集思录旧版对齐）

| 项 | 现状 | 改进 |
|----|------|------|
| 汇率 | 固定无 | 按 QDII 类型读表或配置（官方曾用中间价） |
| 仓位 | 95% 固定 | 按基金或类型配置 |
| ETF | 与 LOF 同公式 | 优先 **IOPV** 列；IOPV 溢价与脚本列分工写清 |
| 商品指数 | 会员「参考标的期间涨幅」 | 无替代数据源则保持 `-` |

### 5.5 工程与测试

- **禁止** calc 与 user.js 公式漂移：改 `*-calc.js` 后跑对应 `*.test.js`，并 diff user.js 内联副本。
- 单测应覆盖：**会话日周末**、**停估边界日**、**YY-MM-DD 净值日期**、501305 / 161127 数值样例。
- 文档：改停估规则时更新 **§3.3、§4.4** 与 **版本号**。

---

## 6. 决策记录（摘要）

| 日期 | 决策 |
|------|------|
| v1.4 亚洲 | 引入会话日；`navDate >= sessionDay` 停估；初版停估后溢价对公布净值 |
| v1.4.1 亚洲 | 停估时 **两列 `-`** + title 说明 |
| v1.1 T-1 | 双表 `flex_qdiie`/`flex_qdiic`；T-2×T-1 指数 |
| v1.1 T-1 | `navDate >= sessionDay−1` 停估；初版停估后溢价对 T-2 |
| v1.1.1 T-1 | 停估时 **两列 `-`** + title 说明（周末 T-2 滚日） |
| 未做 | 法定节假日、IOPV 分支、API 请求 |

---

## 7. 快速对照：一行流程

**亚洲 `fillRow`（逻辑顺序）**

1. 列映射 OK？  
2. 指数不可用？ → `-` / `-`  
3. `parseNavDateYmd` + `getSessionDayYmd` → `navDate >= sessionDay`？ → `-` / `-` + 原因  
4. 否则 `calcEstimate` + `calcPremium(price, estimate)`

**T-1 `fillRow`（逻辑顺序）**

1. 列映射 OK？  
2. 指数不可用？ → `-` / `-`  
3. `shouldUseT1IndexEstimate`（`navDate < sessionDay−1`）？ 否 → `-` / `-` + 原因  
4. 否则 `calcT1Estimate` + `calcPremium(price, estimate)`

---

*文档随仓库维护；若与代码不一致，以 `*-calc.js` 与 `fillRow` 为准，并应修正本文档。*
